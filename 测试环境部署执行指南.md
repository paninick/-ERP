# ERP 项目 - 测试环境部署执行指南

**日期**：2026-04-02  
**阶段**：上线准备阶段 - 测试环境部署执行

---

## 一、部署前准备

### 1.1 环境要求检查清单

| 检查项 | 要求版本 | 检查命令 | 状态 |
|--------|---------|---------|------|
| **JDK** | 17 或更高 | `java -version` | ⏳ |
| **Maven** | 3.6 或更高 | `mvn -version` | ⏳ |
| **MySQL** | 8.0 或更高 | `mysql --version` | ⏳ |
| **Redis** | 6.0 或更高 | `redis-cli --version` | ⏳ |
| **Git** | 任意 | `git --version` | ⏳ |

### 1.2 项目配置检查

| 配置项 | 位置 | 说明 |
|--------|------|------|
| **数据库配置** | `ruoyi-admin/src/main/resources/application-druid.yml` | MySQL 连接配置 |
| **Redis 配置** | `ruoyi-admin/src/main/resources/application.yml` | Redis 连接配置 |
| **文件上传路径** | `ruoyi-admin/src/main/resources/application.yml` | `ruoyi.profile` |

---

## 二、数据库部署步骤

### 2.1 创建数据库

```sql
-- 创建数据库
CREATE DATABASE IF NOT EXISTS `ry-vue` 
DEFAULT CHARACTER SET utf8mb4 
COLLATE utf8mb4_general_ci;

-- 使用数据库
USE `ry-vue`;
```

### 2.2 导入基础数据

按顺序执行以下 SQL 脚本：

| 脚本名称 | 位置 | 说明 |
|---------|------|------|
| 1. ry_20260321.sql | `sql/` | 若依基础数据（用户、角色、菜单、字典） |
| 2. quartz.sql | `sql/` | 定时任务表结构 |
| 3. init.sql | `ruoyi-demo/src/main/resources/sql/` | Demo 模块数据（订单、款式、外发、排程、报工） |

**导入命令示例**：
```bash
# 方式1：使用 MySQL 命令行
mysql -u root -p ry-vue < sql/ry_20260321.sql
mysql -u root -p ry-vue < sql/quartz.sql
mysql -u root -p ry-vue < ruoyi-demo/src/main/resources/sql/init.sql

# 方式2：使用 MySQL Workbench 或 Navicat
# 打开 SQL 文件，选择 ry-vue 数据库，执行脚本
```

### 2.3 验证数据库

```sql
-- 检查表是否创建成功
SHOW TABLES;

-- 验证基础数据
SELECT * FROM sys_user;
SELECT * FROM sys_role;
SELECT * FROM sys_menu;

-- 验证 Demo 模块数据
SELECT * FROM demo_order;
SELECT * FROM demo_style;
SELECT * FROM demo_outsource;
SELECT * FROM demo_schedule;
SELECT * FROM demo_report;
```

---

## 三、应用服务部署步骤

### 3.1 编译项目

```bash
# 进入项目根目录
cd d:\erp\RuoYi-Vue

# 清理并编译项目
mvn clean install -DskipTests

# 或者只编译不跳过测试（较慢）
mvn clean install
```

**预期输出**：
```
[INFO] BUILD SUCCESS
[INFO] ------------------------------------------------------------------------
[INFO] Total time:  03:45 min
[INFO] Finished at: 2026-04-02T10:30:00+08:00
```

### 3.2 检查编译结果

```bash
# 检查 JAR 文件是否生成
dir ruoyi-admin\target

# 应该看到：
# ruoyi-admin.jar
```

### 3.3 配置应用

修改配置文件（如需要）：

**数据库配置**（`ruoyi-admin/src/main/resources/application-druid.yml`）：
```yaml
spring:
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/ry-vue?useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=true&serverTimezone=GMT%2B8
    username: root
    password: your_password  # 修改为您的密码
```

**Redis 配置**（`ruoyi-admin/src/main/resources/application.yml`）：
```yaml
spring:
  data:
    redis:
      host: localhost
      port: 6379
      password:  # 如果有密码请填写
      database: 0
```

**文件上传路径**（`ruoyi-admin/src/main/resources/application.yml`）：
```yaml
ruoyi:
  profile: D:/ruoyi/uploadPath  # 修改为您的路径
```

### 3.4 启动应用服务

```bash
# 方式1：使用 Maven 启动（开发环境）
cd ruoyi-admin
mvn spring-boot:run

# 方式2：使用 JAR 包启动（生产/测试环境）
java -jar ruoyi-admin/target/ruoyi-admin.jar

# 方式3：指定配置文件启动
java -jar ruoyi-admin/target/ruoyi-admin.jar --spring.profiles.active=test
```

### 3.5 验证启动

**检查日志**：
```
  .   ____          _            __ _ _
 /\\ / ___'_ __ _ _(_)_ __  __ _ \ \ \ \
( ( )\___ | '_ | '_| | '_ \/ _` | \ \ \ \
 \\/  ___)| |_)| | | | | || (_| |  ) ) ) )
  '  |____| .__|_| |_|_| |_\__, | / / / /
 =========|_|==============|___/=/_/_/_/
 :: Spring Boot ::               (v4.0.3)

...
(success) =======================================
(success) 若依启动成功
(success) =======================================
...
```

**访问应用**：
- 前端地址：http://localhost:80
- 后端地址：http://localhost:8080
- API 文档：http://localhost:8080/swagger-ui/index.html
- 默认账号：admin/admin123

---

## 四、测试数据验证

### 4.1 数据完整性检查

| 检查表 | 预期记录数 | SQL 命令 |
|--------|----------|---------|
| sys_user | ≥1 | `SELECT COUNT(*) FROM sys_user;` |
| sys_role | ≥1 | `SELECT COUNT(*) FROM sys_role;` |
| sys_menu | ≥1 | `SELECT COUNT(*) FROM sys_menu;` |
| demo_order | 2 | `SELECT COUNT(*) FROM demo_order;` |
| demo_style | 2 | `SELECT COUNT(*) FROM demo_style;` |
| demo_outsource | 2 | `SELECT COUNT(*) FROM demo_outsource;` |
| demo_schedule | 3 | `SELECT COUNT(*) FROM demo_schedule;` |
| demo_report | 2 | `SELECT COUNT(*) FROM demo_report;` |

### 4.2 金额精度验证

```sql
-- 验证 BigDecimal 精度（2位小数）
SELECT order_no, fob_price, cost, profit, profit_rate
FROM demo_order;

-- 验证款式成本精度
SELECT style_no, fabric_cost, accessory_cost, cmt_cost, standard_cost
FROM demo_style;

-- 验证外发加工费精度
SELECT id, unit_price, total_amount
FROM demo_outsource;
```

### 4.3 日期格式验证

```sql
-- 验证日期格式
SELECT order_no, create_time, update_time
FROM demo_order;

SELECT style_no, create_time, update_time
FROM demo_style;

SELECT id, work_day, create_time, update_time
FROM demo_schedule;

SELECT id, work_date, create_time, update_time
FROM demo_report;
```

---

## 五、功能测试

### 5.1 用户登录测试

1. 访问 http://localhost:80
2. 输入账号：admin
3. 输入密码：admin123
4. 点击"登录"按钮
5. 验证是否成功登录并进入首页

### 5.2 系统管理测试

| 功能 | 测试步骤 |
|------|---------|
| 用户管理 | 系统管理 → 用户管理 → 查询、新增、编辑、删除 |
| 角色管理 | 系统管理 → 角色管理 → 查询、新增、编辑、删除 |
| 菜单管理 | 系统管理 → 菜单管理 → 查询、新增、编辑、删除 |
| 字典管理 | 系统管理 → 字典管理 → 查询、新增、编辑、删除 |

### 5.3 Demo 模块测试

| 模块 | 测试步骤 |
|------|---------|
| 订单管理 | Demo 模块 → 订单管理 → 查询、新增、编辑、删除、导出 |
| 款式管理 | Demo 模块 → 款式管理 → 查询、新增、编辑、删除、导出 |
| 外发管理 | Demo 模块 → 外发管理 → 查询、新增、编辑、删除、导出 |
| 排程管理 | Demo 模块 → 排程管理 → 查询、新增、编辑、删除、导出 |
| 报工管理 | Demo 模块 → 报工管理 → 查询、新增、编辑、删除、导出 |

### 5.4 导出功能测试

1. 进入订单管理页面
2. 点击"导出"按钮
3. 选择导出选项
4. 点击"确定"
5. 验证 Excel 文件是否正常下载和打开

---

## 六、常见问题处理

### 6.1 数据库连接失败

**问题**：`Communications link failure`

**解决方案**：
1. 检查 MySQL 服务是否启动
2. 检查数据库连接配置（URL、用户名、密码）
3. 检查防火墙设置
4. 确认数据库 `ry-vue` 是否已创建

### 6.2 Redis 连接失败

**问题**：`Unable to connect to Redis`

**解决方案**：
1. 检查 Redis 服务是否启动
2. 检查 Redis 连接配置（地址、端口、密码）
3. 检查防火墙设置

### 6.3 端口被占用

**问题**：`Port 8080 was already in use`

**解决方案**：
1. 查找占用端口的进程：`netstat -ano | findstr :8080`
2. 结束进程：`taskkill /PID <进程ID> /F`
3. 或者修改配置文件中的端口号

### 6.4 文件上传失败

**问题**：文件上传目录不存在

**解决方案**：
1. 检查 `ruoyi.profile` 配置的路径
2. 手动创建该目录
3. 确保应用有该目录的读写权限

---

## 七、部署检查清单

### 7.1 部署前检查

| 检查项 | 状态 | 备注 |
|--------|------|------|
| JDK 17 安装 | ⏳ | |
| Maven 安装 | ⏳ | |
| MySQL 8.0 安装 | ⏳ | |
| Redis 6.0 安装 | ⏳ | |
| 数据库 ry-vue 创建 | ⏳ | |
| 基础数据导入 | ⏳ | |
| Demo 模块数据导入 | ⏳ | |

### 7.2 部署后检查

| 检查项 | 状态 | 备注 |
|--------|------|------|
| 项目编译成功 | ⏳ | |
| JAR 文件生成 | ⏳ | |
| 应用启动成功 | ⏳ | |
| 端口 8080 监听正常 | ⏳ | |
| 用户登录成功 | ⏳ | |
| 菜单加载正常 | ⏳ | |
| Demo 模块可用 | ⏳ | |
| 导出功能正常 | ⏳ | |

---

## 八、附录

### 8.1 相关文档

- [系统上线准备计划.md](file:///d:/erp/系统上线准备计划.md)
- [测试环境部署方案与执行报告.md](file:///d:/erp/测试环境部署方案与执行报告.md)
- [测试数据准备方案与执行报告.md](file:///d:/erp/测试数据准备方案与执行报告.md)
- [项目文档索引.md](file:///d:/erp/项目文档索引.md)

---

**指南结束**

**创建人**：Trae Code CN  
**日期**：2026-04-02  
**状态**：✅ 测试环境部署执行指南已创建，等待执行！
